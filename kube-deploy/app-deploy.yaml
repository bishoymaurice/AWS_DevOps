apiVersion: apps/v1
kind: Deployment
metadata:
  name: sampleapp-deployment-TARGET_TAG
  labels:
    app: sampleapp-TARGET_TAG
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sampleapp-TARGET_TAG
  template:
    metadata:
      labels:
        app: sampleapp-TARGET_TAG
    spec:
      containers:
      - name: sampleapp-TARGET_TAG
        image: bishoy/sampleapp:TARGET_TAG
        ports:
        - containerPort: 80